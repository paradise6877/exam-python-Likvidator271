# 📝 ЭКЗАМЕН  
## по дисциплине «Основы программирования (Python)»

---
### 📌 Разрешено
- стандартный Python
- логические операции
- методы строк и списков
- метод `split()` для ввода данных
- ввод нескольких значений в одной строке через `map()`

---

### ❌ Запрещено
- `if`, `else`
- `for`, `while`
- списковые включения
- метод `join()`

---
## ⚠️ ВАЖНО

**Использование запрещённых конструкций ведёт к снижению оценки**

**Формат вывода должен строго соответствовать условию**

**Лишний вывод считается ошибкой**

## ❗ Строгие запреты и предупреждения

🚫 Запрещено:
- использовать LLM и нейросети:
  - ChatGPT
  - DeepSeek
  - Grok
  - Copilot
  - любые другие ИИ-инструменты
  - пользоваться готовыми шаблонами
  - копировать код у других студентов
   
📉 **За разговоры во время экзамена итоговая оценка снижается**

❗ **Произвольная смена варианта задания**  
➡️ автоматическая оценка **2 (неудовлетворительно)**

---

## 🔐 Требования к работе с Git

После завершения работы и выполнения команды:

```
git push
```
**Cтудент ОБЯЗАН:**

-Удалить локальную копию проекта со своего компьютера

❗ Это необходимо для предотвращения передачи экзаменационного задания
другим студентам.

📉 В случае выявления факта передачи проекта:

работа аннулируется

итоговая оценка — 2 (неудовлетворительно)

**Решение задач следует писать в файле student_solution.py, номер задачи должен соответсвовать номеру task**

## 🔹 ЧАСТЬ 1. Булевый тип и строки

### **Задание 1. Логические операции и строки**

Вводится одна строка.  
Известно, что она содержит **две подстроки**, разделённые символом `,`.

**Пример ввода**
```
hello,world
```

Выведите три логических значения (`True` или `False`), каждое с новой строки:

1. длина первой подстроки больше второй  
2. подстроки равны  
3. вторая подстрока содержится в первой  

**Подсказка:** можно использовать `find`, `count`, `index`, срезы.

---

### **Задание 2. Методы строк**

Вводится одна строка.

Выведите (каждое значение с новой строки):

1. строку без пробелов в начале и конце  
2. количество символов в строке  
3. количество букв `a`  
4. строку, где все буквы `a` заменены на `@`  
5. `True`, если строка начинается с заглавной буквы, иначе `False`

**Разрешённые методы:**  
`strip`, `len`, `count`, `replace`, `istitle`, `isupper`

---

### **Задание 3. Срезы и регистр**

Вводится одна строка.

Выведите:

1. строку без первого и последнего символа  
2. каждый второй символ строки  
3. строку в нижнем регистре, записанную в обратном порядке  

---

## 🔹 ЧАСТЬ 2. Списки и методы списков

### **Задание 4. Работа с числовым списком**

Вводится строка чисел, разделённых пробелом.

**Пример ввода**
```
48193
```

Получите список чисел с помощью `split()` и `map()`.

Выведите:

1. отсортированный список  
2. сумму всех элементов  
3. минимальное и максимальное число  

---


## 🔹 ЧАСТЬ 3. Комбинированное задание ⭐

**Задание 5. Строки + логика (без if)**

Вводится одна строка.

Выведите True, если одновременно выполняются условия:

строка является палиндромом (без учёта регистра)

строка не содержит пробелов

Иначе выведите False.

Подсказка:
можно использовать lower, replace, срезы и логическое and.

**Задание 6. Перевод числа в 16-ричный формат**
Вводится одно целое число.
Выведите:

1. число в 16-ричной системе (строка, без префикса 0x)

2. количество символов в 16-ричном представлении

3. True, если 16-ричное число содержит букву a

**Пример ввода**
```
255
```

**Пример вывода**
```
ff
2
False
```
Для перевода числа в 16-ричную систему используете функцию **hex**.

## Задание 7. Название месяца по номеру ##
Список месяцев:
```
months = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec']
```
Вводится число от 1 до 12.
Выведите название месяца, соответствующее номеру.

**Пример ввода**
```
1
```

**Пример вывода**
```
Jan
```
## ⚠️ ВАЖНО

**Использование запрещённых конструкций ведёт к снижению оценки**

**Формат вывода должен строго соответствовать условию**

**Лишний вывод считается ошибкой**

## Не забудь команды для git:
```
git clone -клонировать репозитарий
git status - проверить состояние файлов перед индексом и коммитом
git add <имя файла> - добавить файл в индекс
git commit -m"собщение" - добавить файлы и собщение в репозитарий
git push origin main- "запушить" отправить репозитарий на удаленный сервер (Github)
```
